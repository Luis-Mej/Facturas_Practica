namespace Practica.Extensiones
{
    public static class Extensions 
    {
        public static string GenerarNombreUsuario (this string nombresCompletos)
        {
            string[] nombres = nombresCompletos.Split(" ");

            if(nombres.Count > 2){
                throw new Argu
            }

        }
    }
}